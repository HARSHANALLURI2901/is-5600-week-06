import React from "react";

export default function Button({text, handleClick}) {
  return (
    <a href="a" className="f5 no-underline black bg-animate hover-bg-black">(text)
      <span className="pl1">(text)</span>
      </a>      
  )
}